---
import BaseLayout from '../layouts/BaseLayout.astro';
import Subscribe from '../components/Subscribe.astro';
import { getPublishedWriting } from '../lib/content';

const posts = (await getPublishedWriting())
  .sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf())
  .slice(0, 5);

const shortDate = (d: Date) =>
  d.toLocaleDateString('en-US', { month: 'short', day: 'numeric' });
---

<BaseLayout>
  <section>
    <h1>Software engineer, photographer, gourmand.</h1>
    <p>
      Stockholm. Writing about craft, food, the inner work, and whatever else is worth saying.
    </p>
  </section>

  <section>
    <h2>Recent writing</h2>
    <ul class="archive-list">
      {posts.map((post) => (
        <li>
          <a href={`/writing/${post.id}/`}>{post.data.title}</a>
          <span class="archive-date">{shortDate(post.data.pubDate)}</span>
        </li>
      ))}
    </ul>
    <p style="margin-top: 1.5rem;"><a href="/writing">All writing →</a></p>
  </section>

  <Subscribe />
</BaseLayout>
